Flask==3.0.3
Werkzeug==3.0.4

# PyTorch - Use CPU-only version for production to reduce size (~200MB vs ~3GB)
# For development with GPU, install separately: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For production: Use requirements-prod.txt which has CPU-only PyTorch
torch>=2.0.0
torchvision>=0.15.0

Pillow>=8.0.0
scikit-learn>=1.0.0
matplotlib>=3.3.0
seaborn>=0.11.0
numpy>=1.21.0
scipy>=1.10.0
supabase>=2.0.0
python-dotenv>=1.0.0
ultralytics>=8.0.0
tqdm>=4.65.0

