# Production requirements - Lightweight version for deployment
# Uses CPU-only PyTorch to reduce size from ~3GB to ~200MB

Flask==3.0.3
Werkzeug==3.0.4

# CPU-only PyTorch (much smaller than CUDA version)
# This reduces total size from ~3GB to ~200MB
torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0,<1.0.0 --index-url https://download.pytorch.org/whl/cpu

# Core dependencies
Pillow>=8.0.0
numpy>=1.21.0
scipy>=1.10.0

# Database
supabase>=2.0.0
python-dotenv>=1.0.0

# Optional ML dependencies (can be removed if not using ML features)
# Uncomment if you need these:
# scikit-learn>=1.0.0
# matplotlib>=3.3.0
# seaborn>=0.11.0

# YOLO (Ultralytics) - optional, loads lazily
# ultralytics>=8.0.0

